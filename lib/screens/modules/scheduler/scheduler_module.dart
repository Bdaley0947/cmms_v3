import 'package:flutter/material.dart';

class SchedulerModule extends StatelessWidget {
  const SchedulerModule({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Scheduler view placeholder',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
