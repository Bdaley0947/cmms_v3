import 'package:flutter/material.dart';

class AssetModule extends StatelessWidget {
  const AssetModule({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Asset Module'));
  }
}
